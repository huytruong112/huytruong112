version: '3.8'

services:
  app:
    build: .
    container_name: 3x-ui-service-manager
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - DB_PATH=/app/data/database.sqlite
